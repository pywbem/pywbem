# Pip requirements file for development dependencies.


# Direct dependencies for development
# (must be consistent with minimum-constraints-develop.txt)

# Tox
tox>=4.15.0

# Package builder
build>=1.1.1

# Cythonize
Cython>=3.0.0

# Vendorize
vendorize>=0.3.0

# Change log manager
towncrier>=22.8.0

# Test tools
pytest>=8.4.0; python_version == '3.9'
pytest>=9.0.3; python_version >= '3.10'
requests-mock>=1.6.0
testfixtures>=6.9.0
tzdata>=2024.1; sys_platform == 'win32'

# FormEncode (used for XML comparisons in unit tests)
FormEncode>=2.1.1

# Lxml (used in unit tests)
lxml>=6.1.0

# Easy-server (used in end2end tests)
pytest-easy-server>=0.8.0

# Yagot (used in leak tests)
yagot>=0.5.0

# Virtualenv (used in install tests)
virtualenv>=21.3.0

# Tabulate (used in manual tests)
tabulate>=0.8.3

# Coverage reporting tools
coverage>=7.8.0
coveralls>=4.0.1; python_version == '3.9'
coveralls>=4.0.2; python_version >= '3.10'
pytest-cov>=4.0.0

# Sphinx doc builder and optional extensions
#x Sphinx>=8.0.0 fails on autodocsumm with TypeError: "cannot unpack non-iterable ObjectMember object"
#x Sphinx>=7.2.4,<8.0.0
Sphinx>=7.2.4
autodocsumm>=0.2.12
sphinx-git>=10.1.1
sphinx-rtd-theme>=2.0.0

# Package dependency checkers
pipdeptree>=2.24.0
pip-check-reqs>=2.5.1

# Safety checker
safety>=3.8.1

# Flake8 checker
flake8>=7.0.0

# Ruff checker
ruff>=0.16.0

# PyLint checker
pylint>=3.3.3; python_version == '3.9'
pylint>=4.0.4; python_version >= '3.10'

# Bandit checker
bandit>=1.7.8

# Jupyter notebook
# TODO: Re-enable for Python 3.13 on Windows once pywin32 install issue is solved, see
#       https://github.com/mhammond/pywin32/issues/2367
notebook>=7.5.6; sys_platform != 'win32' or python_version <= '3.12'
jupyter>=1.1.1; sys_platform != 'win32' or python_version <= '3.12'

# Performance profiling tools
pyinstrument>=4.7.3


# Indirect dependencies for development that are needed for some reason
# (must be consistent with minimum-constraints-develop.txt)
