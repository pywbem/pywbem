# Python package requirements for pywbem usage.
# Pip processes the packages in order of appearance.

epydoc>=3.0.1
docutils>=0.12
httpretty
lxml
# lxml requires these linux packages before installing it: libxml2-dev libxslt1-dev
